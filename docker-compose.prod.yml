services:
  badge-creator:
    image: caddy:2
    container_name: badge-creator
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_HOST_PORT:-8789}:8789"
    volumes:
      - ./:/srv:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
